# ============================================================
# Celestra Research AI — Frontend environment
# ============================================================

# Base URL of the deployed FastAPI backend (no trailing /api/v1).
# Defaults to the Render host below if unset.
NEXT_PUBLIC_API_URL=https://celestra-research-api.onrender.com

# Demo tenant the dashboard auto-provisions for read access.
# (Optional — sensible defaults are used if omitted.)
NEXT_PUBLIC_DEMO_EMAIL=demo@celestra.ai
NEXT_PUBLIC_DEMO_PASSWORD=celestra-demo-2026
